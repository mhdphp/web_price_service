{% extends "base.jinja2" %}

{% block content %}

    <div class="page-header">
        <h2>Create Store</h2>
    </div>
    <div class="form-size">
        <form method="post">
            <div class="form-group">
                <label for="name">Store Name</label>
                <input type="text" id="name" name="name" placeholder="Store Name" class="form-control">
            </div>

            <div class="form-group">
                <label for="url_prefix">Store URL Prefix</label>
                <input type="text" id="url_prefix" name="url_prefix"
                       placeholder="http://..." class="form-control" aria-describedby="storeURLPrefixHelp">
                <span id="storeURLPrefixHelp" class="help-block">
                   What characters do URLs of all items in this online store start with?
                </span>
            </div>

            <div class="form-group">
                <label for="tag_name">Tag Name</label>
                <input type="text" id="tag_name" name="tag_name"
                       class="form-control" placeholder="span" value="span"
                       aria-describedby="storeTagNameHelp">
                <span id="storeTagNameHelp" class="help-block">
                   What is the HTML tag of the item that contains the price? (usually <code>span</code>)
               </span>
            </div>

            <div class="form-group">
                <label for="query">Search query</label>
                <input type="text" id="query" name="query"
                       class="form-control" placeholder='{"id":"price-now"}' value='{"id":"price-now"}'
                       aria-describedby="queryHelpBlock">
                <span id="queryHelpBlock" class="help-block">
                   What JSON query would we have to do to find specific tag with the price?
                   Remember to use double quotations.
                </span>
            </div>
            <button type="submit" class="btn btn-primary">Create</button>
        </form>
    </div>

{% endblock %}